tags:
  - WeatherItem
summary: Get a specific weather report for a location
parameters:
  - name: location
    in: path
    description: ID of the location
    required: true
    type: integer
responses:
  200:
    description: Weather report for the location
    schema:
      $ref: '#/definitions/WeatherReport'